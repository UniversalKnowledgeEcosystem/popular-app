import {NextRequest,NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";export const runtime="nodejs";function db(){const u=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw Error("Supabase não configurado");return createClient(u,k,{auth:{persistSession:false}})}function tel(v:unknown){const t=String(v||"").replace(/\D/g,"");return t.length>=10&&t.length<=13?t:null}function av(d:number,m:number){return m>=1&&m<=12&&d>=1&&d<=new Date(2024,m,0).getDate()}export async function GET(req:NextRequest){try{const t=tel(req.nextUrl.searchParams.get("telefone"));if(!t)return NextResponse.json({error:"WhatsApp inválido"},{status:400});const d=db(),{data:c,error}=await d.from("clientes_fidelidade").select("id,nome,telefone,pontos,total_pedidos,total_gasto,aniversario_dia,aniversario_mes,aniversario_verificado,aniversario_ultimo_uso_ano").eq("telefone",t).maybeSingle();if(error)throw error;if(!c)return NextResponse.json({cliente:null});const{data:h}=await d.from("historico_fidelidade").select("tipo,descricao,criado_em").eq("cliente_id",c.id).in("tipo",["aniversario_aprovado","aniversario_recusado"]).order("criado_em",{ascending:false}).limit(1);const hoje=new Date(),dia=Number(new Intl.DateTimeFormat("en-US",{day:"numeric",timeZone:"America/Sao_Paulo"}).format(hoje)),mes=Number(new Intl.DateTimeFormat("en-US",{month:"numeric",timeZone:"America/Sao_Paulo"}).format(hoje)),ano=Number(new Intl.DateTimeFormat("en-US",{year:"numeric",timeZone:"America/Sao_Paulo"}).format(hoje)),benef=!!c.aniversario_verificado&&Number(c.aniversario_dia)===dia&&Number(c.aniversario_mes)===mes&&Number(c.aniversario_ultimo_uso_ano)!==ano;return NextResponse.json({cliente:c,beneficio_aniversario_disponivel:benef,desconto_aniversario:benef?10:0,ultima_verificacao_aniversario:h?.[0]||null})}catch{return NextResponse.json({error:"Não foi possível carregar sua conta"},{status:500})}}export async function POST(req:NextRequest){try{const b=await req.json(),t=tel(b.telefone),dia=Number(b.dia),mes=Number(b.mes);if(!t)return NextResponse.json({error:"WhatsApp inválido"},{status:400});if(!av(dia,mes))return NextResponse.json({error:"Data de aniversário inválida"},{status:400});const d=db(),{data:c,error}=await d.from("clientes_fidelidade").select("id,aniversario_dia,aniversario_mes,aniversario_verificado").eq("telefone",t).maybeSingle();if(error)throw error;if(!c)return NextResponse.json({error:"Faça ao menos um pedido antes de cadastrar o aniversário."},{status:404});if(c.aniversario_verificado&&(Number(c.aniversario_dia)!==dia||Number(c.aniversario_mes)!==mes))return NextResponse.json({error:"Aniversário já verificado. Para alterar, fale com a Popular."},{status:409});const u=await d.from("clientes_fidelidade").update({aniversario_dia:dia,aniversario_mes:mes,...(!c.aniversario_verificado?{aniversario_verificado:false,aniversario_verificado_em:null}:{})}).eq("id",c.id);if(u.error)throw u.error;return NextResponse.json({ok:true,mensagem:c.aniversario_verificado?"Aniversário já está verificado.":"Cadastro enviado. Apresente um documento oficial com foto na Popular para conferência."})}catch{return NextResponse.json({error:"Não foi possível cadastrar o aniversário"},{status:500})}}
