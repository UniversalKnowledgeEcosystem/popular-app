import{NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";export const runtime="nodejs";function db(){const u=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw Error("Supabase não configurado");return createClient(u,k,{auth:{persistSession:false}})}export async function GET(){try{const{data,error}=await db().from("cardapio_config").select("produto_id,nome,preco,disponivel,destaque,preco_promocional,atualizado_em");if(error){if(String(error.message).toLowerCase().includes("cardapio_config"))return NextResponse.json({config:[],configurado:false});throw error}return NextResponse.json({config:data||[],configurado:true},{headers:{"Cache-Control":"no-store"}})}catch{return NextResponse.json({config:[],configurado:false},{status:200})}}
