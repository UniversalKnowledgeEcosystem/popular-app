"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{useCart}from"../context/CartContext";import MascotMoment from"../components/MascotMoment";import{formatPrice}from"../../utils/format";const K="popular_dados_cliente",MIN=20;type D={nome:string;telefone:string;endereco:string;tipo:"entrega"|"retirada";pagamento:string};function soNumeros(v:string){return v.replace(/\D/g,"").slice(0,13)}function telBR(v:string){let t=soNumeros(v);if(t.startsWith("55")&&t.length>=12)t=t.slice(2);if(t.startsWith("0")&&t.length>=11)t=t.slice(1);if(!/^\d{10,11}$/.test(t))return null;const ddd=Number(t.slice(0,2)),n=t.slice(2);if(ddd<11||ddd>99||/^(\d)\1+$/.test(t)||/^(\d)\1+$/.test(n))return null;if(n.length===9&&!/^9\d{8}$/.test(n))return null;if(n.length===8&&!/^[2-5]\d{7}$/.test(n))return null;return t}function mascara(v:string){const t=soNumeros(v).replace(/^55(?=\d{10,11}$)/,"").slice(0,11);if(t.length<=2)return t;if(t.length<=6)return`(${t.slice(0,2)}) ${t.slice(2)}`;if(t.length<=10)return`(${t.slice(0,2)}) ${t.slice(2,6)}-${t.slice(6)}`;return`(${t.slice(0,2)}) ${t.slice(2,7)}-${t.slice(7)}`}function semAcento(v:string){return v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase()}function elegivelNome(nome:string){const n=semAcento(nome.split(" • ")[0]);if(/sorvete|milk\s*shake|milkshake|acai/.test(n))return false;return /misto|x-|big|lanche|hamburguer|burger|frita|batata|refrigerante|coca|kuat|guarana|monster|suco|vitamina/.test(n)}export default function Carrinho(){const{items,total,adicionar,diminuir,remover,limpar}=useCart();const[nome,setNome]=useState(""),[telefone,setTelefone]=useState(""),[endereco,setEndereco]=useState(""),[pagamento,setPagamento]=useState("Pix"),[obs,setObs]=useState("");const[tipo,setTipo]=useState<"entrega"|"retirada">("entrega");const[enviando,setEnviando]=useState(false),[revisao,setRevisao]=useState(false),[erroTel,setErroTel]=useState(""),[sucesso,setSucesso]=useState(false);const totalElegivel=items.filter(i=>elegivelNome(i.nome)).reduce((s,i)=>s+Number(i.preco)*Number(i.quantidade),0);useEffect(()=>{try{const d=JSON.parse(localStorage.getItem(K)||"null")as Partial<D>|null;if(d){setNome(String(d.nome||""));setTelefone(mascara(String(d.telefone||"")));setEndereco(String(d.endereco||""));setTipo(d.tipo==="retirada"?"retirada":"entrega");setPagamento(String(d.pagamento||"Pix"))}}catch{}},[]);function validar(){if(!nome.trim()){alert("Digite seu nome.");return false}if(tipo==="entrega"&&!endereco.trim()){alert("Digite o endereço completo para entrega.");return false}const t=telBR(telefone);if(!t){setErroTel("Número inválido. Informe DDD + telefone. Ex.: (38) 99999-9999.");alert("Número de telefone inválido. Confira o DDD e o número antes de continuar.");return false}setErroTel("");return true}async function finalizar(){if(enviando||!validar())return;const tel=telBR(telefone);if(!tel)return;setEnviando(true);try{const itens=items.map(i=>({produto_id:String(i.id),nome:i.nome,preco:Number(i.preco),quantidade:Number(i.quantidade)}));const r=await fetch("/api/pedidos",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({whatsapp:tel,nome_cliente:nome.trim(),total,itens,tipo,endereco:endereco.trim()})}),d=await r.json();if(!r.ok){if(String(d.error||"").toLowerCase().includes("telefone"))setErroTel(String(d.error));alert(d.error||"Não foi possível registrar o pedido.");return}localStorage.setItem(K,JSON.stringify({nome:nome.trim(),telefone:tel,endereco:endereco.trim(),tipo,pagamento}));const id=String(d.pedido?.pedido_id||"");if(d.perfil_token&&id)localStorage.setItem("popular_perfil_acesso",JSON.stringify({telefone:tel,pedido_id:id,token:String(d.perfil_token)}));const oficiais=Array.isArray(d.pedido?.itens)?d.pedido.itens:itens,totalOficial=Number(d.total_oficial??d.pedido?.total??total),linhas=oficiais.map((i:any)=>`• ${i.quantidade}x ${i.nome} — ${formatPrice(Number(i.preco)*Number(i.quantidade))}`).join("\n"),fid=d.elegivel_selo?"⭐ *Elegível para 1 selo após validação.*":"ℹ️ *Este pedido não gera selo.*",mensagem=`🍔 *NOVO PEDIDO - POPULAR*\n\n🆔 ${id}\n👤 ${nome}\n📱 ${tel}\n${tipo==="entrega"?`🛵 ${endereco}`:"🏪 Retirada no local"}\n💳 ${pagamento}\n\n🧾 *PEDIDO*\n${linhas}\n\n💰 *TOTAL: ${formatPrice(totalOficial)}*\n${fid}${obs.trim()?`\n📝 ${obs}`:""}`;limpar();setRevisao(false);setSucesso(true);window.open(`https://wa.me/5538991429166?text=${encodeURIComponent(mensagem)}`,"_blank")}catch{alert("Não foi possível registrar o pedido.")}finally{setEnviando(false)}}const campo="w-full bg-zinc-800 border border-zinc-700 focus:border-yellow-400 outline-none rounded-xl p-3";if(sucesso)return <main className="min-h-screen bg-zinc-950 text-white p-6 flex items-center justify-center"><div className="max-w-md w-full bg-zinc-900 border border-green-500/30 rounded-3xl p-7 text-center"><div className="text-6xl">✅</div><h1 className="text-3xl font-black mt-3">Pedido preparado!</h1><p className="text-zinc-400 mt-2">Confira o WhatsApp e envie a mensagem para a Popular.</p><Link href="/cardapio" className="block bg-yellow-400 text-black py-4 rounded-xl font-black mt-5">🍔 Fazer novo pedido</Link></div></main>;if(!items.length)return <main className="min-h-screen bg-zinc-950 text-white p-6"><div className="max-w-md mx-auto"><h1 className="text-3xl font-black">🛒 Seu carrinho</h1><div className="mt-8"><MascotMoment titulo="Ei, não me deixe sozinho por aqui! 😄" texto="Seu carrinho ainda está vazio. Que tal escolher um lanche caprichado, uma porção de fritas e sua bebida favorita?"/></div><div className="mt-4 bg-yellow-400/10 border border-yellow-400/25 rounded-2xl p-4 text-center"><p className="text-sm text-yellow-200 font-bold">🍔 Monte seu pedido do seu jeito e aproveite as delícias da Popular!</p></div><Link href="/cardapio" className="block text-center mt-5 bg-yellow-400 text-black px-6 py-4 rounded-2xl font-black text-lg">😋 Quero escolher meu pedido →</Link></div></main>;return <main className="min-h-screen bg-zinc-950 text-white p-4 pb-32 max-w-3xl mx-auto"><div className="flex justify-between items-center"><div><p className="text-yellow-400 text-xs font-black">POPULAR</p><h1 className="text-3xl font-black">🛒 Seu carrinho</h1></div><Link href="/cardapio" className="text-sm text-yellow-400 font-bold">+ Adicionar</Link></div><div className="space-y-3 mt-5">{items.map(i=><div key={i.id} className="bg-zinc-900 border border-zinc-800 rounded-2xl p-4"><div className="flex justify-between gap-3"><div><b>{i.nome}</b><p className="text-yellow-400 text-sm">{formatPrice(i.preco)} cada</p></div><button onClick={()=>remover(i.id)} className="w-9 h-9 bg-zinc-800 rounded-xl">×</button></div><div className="flex justify-between items-center mt-3"><div className="bg-zinc-800 rounded-xl"><button onClick={()=>diminuir(i.id)} className="px-4 py-2">−</button><span className="px-2 font-black">{i.quantidade}</span><button onClick={()=>adicionar({id:i.id,nome:i.nome,preco:i.preco})} className="px-4 py-2">+</button></div><b>{formatPrice(i.preco*i.quantidade)}</b></div></div>)}</div><section className="mt-4 bg-zinc-900 border border-zinc-800 rounded-2xl p-4"><div className="flex justify-between text-zinc-400"><span>Subtotal</span><span>{formatPrice(total)}</span></div><div className="flex justify-between text-xl font-black mt-3 pt-3 border-t border-zinc-800"><span>Total</span><span className="text-yellow-400">{formatPrice(total)}</span></div></section><div className={`mt-4 p-4 rounded-2xl border ${totalElegivel>=MIN?"bg-green-500/10 border-green-500/30 text-green-300":"bg-yellow-400/10 border-yellow-400/30 text-yellow-300"}`}><b>{totalElegivel>=MIN?"⭐ Este pedido pode valer 1 selo!":`⭐ Faltam ${formatPrice(Math.max(0,MIN-totalElegivel))} em itens elegíveis para 1 selo`}</b><p className="text-xs mt-1 opacity-80">Contam: lanches, fritas, refrigerantes, sucos e vitaminas.</p></div><section className="mt-4 bg-zinc-900 border border-zinc-800 rounded-3xl p-5 space-y-3"><h2 className="text-xl font-black">Dados do pedido</h2><input value={nome} onChange={e=>setNome(e.target.value)} placeholder="Seu nome" className={campo}/><div><input value={telefone} onChange={e=>{setTelefone(mascara(e.target.value));setErroTel("")}} placeholder="WhatsApp com DDD" inputMode="numeric" maxLength={15} className={`${campo} ${erroTel?"border-red-500":""}`}/>{erroTel&&<p className="text-red-400 text-sm mt-1">⚠️ {erroTel}</p>}</div><div className="grid grid-cols-2 gap-2"><button onClick={()=>setTipo("entrega")} className={`p-3 rounded-xl font-bold ${tipo==="entrega"?"bg-yellow-400 text-black":"bg-zinc-800"}`}>🛵 Entrega</button><button onClick={()=>setTipo("retirada")} className={`p-3 rounded-xl font-bold ${tipo==="retirada"?"bg-yellow-400 text-black":"bg-zinc-800"}`}>🏪 Retirada</button></div>{tipo==="entrega"&&<input value={endereco} onChange={e=>setEndereco(e.target.value)} placeholder="Rua, nº, bairro e referência" className={campo}/>}<select value={pagamento} onChange={e=>setPagamento(e.target.value)} className={campo}><option>Pix</option><option>Dinheiro</option><option>Cartão</option></select><textarea value={obs} onChange={e=>setObs(e.target.value)} placeholder="Observações (opcional)" className={campo}/><button onClick={()=>{if(validar())setRevisao(true)}} className="w-full bg-green-500 text-black py-4 rounded-xl font-black text-lg">Revisar pedido →</button></section>{revisao&&<div className="fixed inset-0 z-[80] bg-black/80 p-4 flex items-center justify-center"><div className="bg-zinc-900 rounded-3xl p-5 w-full max-w-md"><h2 className="text-2xl font-black">Está tudo certo?</h2><div className="mt-3 bg-zinc-950 p-4 rounded-2xl text-sm"><p>👤 {nome}</p><p>📱 {telefone}</p><p>{tipo==="entrega"?`🛵 ${endereco}`:"🏪 Retirada no local"}</p><p>💳 {pagamento}</p><p className="font-black text-yellow-400 pt-2">Total: {formatPrice(total)}</p></div><button disabled={enviando} onClick={finalizar} className="w-full bg-green-500 text-black py-4 rounded-xl font-black mt-4">{enviando?"Preparando pedido...":"✓ Finalizar no WhatsApp"}</button><button onClick={()=>setRevisao(false)} className="w-full bg-zinc-800 py-3 rounded-xl mt-2">← Quero corrigir</button></div></div>}</main>}