"use client";
import{Suspense,useEffect,useMemo,useState}from"react";import Link from"next/link";import{useSearchParams}from"next/navigation";import{products}from"../../data/products";import{formatPrice}from"../../utils/format";import{useCart}from"../context/CartContext";type Item={id:number;nome:string;descricao:string;preco:number;categoria:string;emoji:string;disponivel:boolean;destaque:boolean;normal?:number};const FAV="popular_favoritos",extras=[{n:"Bacon extra",p:3},{n:"Carne extra",p:4},{n:"Queijo extra",p:2},{n:"Ovo extra",p:2}];const dados=[["Bebidas","🥤",[["Coca-Cola lata 350ml",5],["Coca-Cola mini lata",3.5],["Coca-Cola 1 litro",8],["Coca-Cola 2 litros",12],["Kuat Guaraná 2 litros",8.5],["Monster",12]]],["Sucos","🍹",[["Suco médio",6],["Suco grande",7],["Vitamina média",7],["Vitamina grande",8]]],["Açaí","🫐",[["Açaí 200ml",8],["Açaí 300ml",10],["Açaí 400ml",12],["Açaí 500ml",14]]],["Sorvetes","🍦",[["1 bola de sorvete",2.5],["Picolé comum",2],["Picolé de casquinha",3.5]]],["Milk Shakes","🥛",[["Milk shake 300ml",9],["Milk shake 400ml",11],["Milk shake 500ml",13]]]]as const,categorias=["Todos","Favoritos","Lanches","Batatas","Bebidas","Sucos","Açaí","Sorvetes","Milk Shakes"];
function C(){const params=useSearchParams(),{adicionar,quantidadeTotal,total}=useCart();const[busca,setBusca]=useState(""),[categoria,setCategoria]=useState("Todos"),[edit,setEdit]=useState<Item|null>(null),[creme,setCreme]=useState(""),[sem,setSem]=useState<string[]>([]),[add,setAdd]=useState<string[]>([]),[fav,setFav]=useState<number[]>([]),[cfg,setCfg]=useState<any[]>([]),[toast,setToast]=useState("");useEffect(()=>{try{setFav(JSON.parse(localStorage.getItem(FAV)||"[]"))}catch{}const c=params.get("categoria");if(c&&categorias.includes(c))setCategoria(c);fetch("/api/cardapio",{cache:"no-store"}).then(r=>r.json()).then(j=>setCfg(j.config||[])).catch(()=>{})},[params]);const itens:Item[]=useMemo(()=>{let id=10000;const base:Item[]=[...products.map(p=>({id:p.id,nome:p.nome,descricao:p.descricao||"",preco:p.preco,categoria:p.categoria==="Batatas"?"Batatas":"Lanches",emoji:p.categoria==="Batatas"?"🍟":"🍔",disponivel:true,destaque:false})),...dados.flatMap(([cat,emoji,l])=>l.map(([nome,preco])=>({id:id++,nome,descricao:"",preco:Number(preco),categoria:String(cat),emoji:String(emoji),disponivel:true,destaque:false})))];return base.map((i):Item=>{const c=cfg.find((x:any)=>String(x.produto_id)===String(i.id)||String(x.nome).toLowerCase()===i.nome.toLowerCase());if(!c)return i;const promo=c.preco_promocional!=null?Number(c.preco_promocional):null,normal=Number(c.preco);return{...i,preco:promo??normal,normal:promo!=null?normal:undefined,disponivel:c.disponivel!==false,destaque:!!c.destaque}}).sort((a,b)=>Number(b.destaque)-Number(a.destaque))},[cfg]);const filtrados=itens.filter(i=>{const q=busca.toLowerCase();return(categoria==="Todos"||(categoria==="Favoritos"?fav.includes(i.id):i.categoria===categoria))&&(!q||i.nome.toLowerCase().includes(q))});function favorito(id:number){const n=fav.includes(id)?fav.filter(x=>x!==id):[...fav,id];setFav(n);localStorage.setItem(FAV,JSON.stringify(n))}function abrir(i:Item){if(!i.disponivel)return;if(i.categoria!=="Lanches"){adicionar({id:i.id,nome:i.nome,preco:i.preco});return aviso(i.nome)}setEdit(i);setCreme(/^big\b/i.test(i.nome)?"Cheddar":"");setSem([]);setAdd([])}function aviso(n:string){setToast(`✓ ${n} adicionado`);setTimeout(()=>setToast(""),1500)}function montar(){if(!edit)return;const e=extras.filter(x=>add.includes(x.n)),preco=edit.preco+e.reduce((s,x)=>s+x.p,0),det=[creme&&/^big\b/i.test(edit.nome)?creme:"",...sem.map(x=>`sem ${x}`),...e.map(x=>`+ ${x.n}`)].filter(Boolean),nome=`${edit.nome}${det.length?` • ${det.join(" • ")}`:""}`,id=edit.id*100000+Math.abs(nome.split("").reduce((a,c)=>((a<<5)-a+c.charCodeAt(0))|0,0));adicionar({id,nome,preco});setEdit(null);aviso(edit.nome)}const ing=edit?.descricao.split(/[,.]/).map(x=>x.trim()).filter(x=>x&&x.length<24).slice(0,8)||[];return <main className="min-h-screen bg-black text-white pb-40"><div className="sticky top-0 z-40 bg-black/95 border-b border-zinc-800 p-4"><div className="max-w-5xl mx-auto"><div className="flex justify-between"><div><p className="text-yellow-400 text-xs font-black">CARDÁPIO ATUALIZADO</p><h1 className="text-2xl font-black">Popular</h1></div><Link href="/">Início</Link></div><input value={busca} onChange={e=>setBusca(e.target.value)} placeholder="🔎 O que você quer comer?" className="w-full bg-zinc-900 p-3 rounded-xl mt-3"/><div className="flex gap-2 overflow-x-auto pt-3">{categorias.map(c=><button key={c} onClick={()=>setCategoria(c)} className={`whitespace-nowrap px-4 py-2 rounded-full text-sm font-bold ${categoria===c?"bg-yellow-400 text-black":"bg-zinc-900"}`}>{c}</button>)}</div></div></div><div className="max-w-5xl mx-auto p-4 grid sm:grid-cols-2 gap-3">{filtrados.map(i=><article key={i.id} className={`relative rounded-2xl p-4 flex gap-3 border ${i.disponivel?"bg-zinc-900 border-zinc-800":"bg-zinc-950 border-zinc-900 opacity-60"}`}>{i.destaque&&<span className="absolute -top-2 left-3 bg-yellow-400 text-black text-[10px] font-black px-2 py-1 rounded-full">🔥 DESTAQUE</span>}<div className="text-3xl">{i.emoji}</div><div className="flex-1"><div className="flex justify-between"><b>{i.nome}</b><button onClick={()=>favorito(i.id)}>{fav.includes(i.id)?"❤️":"🤍"}</button></div>{!i.disponivel?<p className="text-red-400 font-black text-sm mt-2">● ESGOTADO NO MOMENTO</p>:<>{i.normal&&<span className="text-xs text-zinc-500 line-through mr-2">{formatPrice(i.normal)}</span>}<b className={i.normal?"text-green-400":"text-yellow-400"}>{formatPrice(i.preco)}</b>{i.normal&&<span className="text-[10px] bg-green-500 text-black font-black ml-2 px-2 py-1 rounded-full">OFERTA</span>}</>}</div><button disabled={!i.disponivel} onClick={()=>abrir(i)} className={`w-11 h-11 rounded-xl text-2xl font-black ${i.disponivel?"bg-yellow-400 text-black":"bg-zinc-800 text-zinc-600"}`}>{i.disponivel?"+":"×"}</button></article>)}</div>{toast&&<div className="fixed top-4 z-[90] left-4 right-4 max-w-sm mx-auto bg-green-500 text-black p-3 rounded-xl text-center font-black">{toast}</div>}{edit&&<div className="fixed inset-0 z-[80] bg-black/80 p-4 overflow-y-auto"><div className="max-w-md mx-auto bg-zinc-900 rounded-3xl p-5 my-8"><div className="flex justify-between"><h2 className="text-2xl font-black">{edit.nome}</h2><button onClick={()=>setEdit(null)}>✕</button></div>{/^big\b/i.test(edit.nome)&&<div className="grid grid-cols-2 gap-2 mt-4">{["Cheddar","Catupiry"].map(x=><button key={x} onClick={()=>setCreme(x)} className={`p-3 rounded-xl font-bold ${creme===x?"bg-yellow-400 text-black":"bg-zinc-800"}`}>{x}</button>)}</div>}<section className="mt-5"><b>🚫 Retirar ingredientes</b><div className="flex flex-wrap gap-2 mt-2">{ing.map(x=><button key={x} onClick={()=>setSem(sem.includes(x)?sem.filter(y=>y!==x):[...sem,x])} className={`px-3 py-2 rounded-xl text-xs ${sem.includes(x)?"bg-red-500":"bg-zinc-800"}`}>{x}</button>)}</div></section><section className="mt-5"><b>➕ Adicionais</b>{extras.map(x=><button key={x.n} onClick={()=>setAdd(add.includes(x.n)?add.filter(y=>y!==x.n):[...add,x.n])} className={`w-full flex justify-between p-3 rounded-xl mt-2 ${add.includes(x.n)?"bg-green-500 text-black":"bg-zinc-800"}`}><span>{x.n}</span><b>+ {formatPrice(x.p)}</b></button>)}</section><button onClick={montar} className="w-full bg-yellow-400 text-black p-4 rounded-xl font-black mt-5">Adicionar ao carrinho</button></div></div>}{quantidadeTotal>0&&<Link href="/carrinho" className="fixed bottom-20 left-4 right-4 max-w-md mx-auto bg-yellow-400 text-black rounded-2xl p-4 flex justify-between font-black"><span>🛒 {quantidadeTotal} itens</span><span>{formatPrice(total)} →</span></Link>}</main>}
export default function Cardapio(){return <Suspense><C/></Suspense>}
