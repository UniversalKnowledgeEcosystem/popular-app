"use client";import{useEffect,useState}from"react";const KEY="popular_admin_token";const money=(v:any)=>Number(v||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});export default function Clientes(){const[cs,setCs]=useState<any[]>([]),[q,setQ]=useState(""),[erro,setErro]=useState("");useEffect(()=>{const t=localStorage.getItem(KEY);if(!t){location.href="/admin";return}fetch("/api/admin",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${t}`},body:JSON.stringify({action:"listar_pedidos"})}).then(async r=>{const j=await r.json();if(!r.ok)throw Error(j.error);setCs(j.dashboard?.clientes||[])}).catch(e=>setErro(e.message))},[]);const lista=cs.filter(c=>`${c.nome} ${c.telefone}`.toLowerCase().includes(q.toLowerCase()));return <main className="min-h-screen bg-zinc-950 text-white p-4"><div className="max-w-5xl mx-auto"><header className="flex justify-between"><div><p className="text-yellow-400 text-xs font-black">POPULAR • CRM</p><h1 className="text-3xl font-black">👥 Clientes</h1><p className="text-zinc-500 text-sm">Clique em um cliente para abrir a ficha completa.</p></div><button onClick={()=>location.href="/admin"} className="bg-zinc-800 px-4 h-11 rounded-xl">← Admin</button></header><input value={q} onChange={e=>setQ(e.target.value)} placeholder="🔎 Buscar por nome ou WhatsApp" className="w-full bg-zinc-900 p-4 rounded-xl mt-5"/>{erro&&<p className="text-red-400 mt-3">{erro}</p>}<p className="text-xs text-zinc-500 my-3">{lista.length} cliente(s)</p>{lista.map(c=><button key={c.id} onClick={()=>location.href=`/admin/clientes/${c.id}`} className="w-full text-left bg-zinc-900 border border-zinc-800 p-4 rounded-2xl mb-2 hover:border-yellow-400"><div className="flex justify-between gap-2"><div><b className="text-lg">👤 {c.nome}</b><p className="text-sm text-zinc-500">📱 {c.telefone}</p></div><span className="text-yellow-400 font-black">›</span></div><div className="grid grid-cols-3 gap-2 mt-3 text-xs"><span>🛍️ {c.total_pedidos||0} pedidos</span><span>⭐ {c.pontos||0}/10 selos</span><span className="text-right">💰 {money(c.total_gasto)}</span></div>{c.aniversario_dia&&<p className="text-xs mt-2 text-zinc-400">🎂 {String(c.aniversario_dia).padStart(2,"0")}/{String(c.aniversario_mes).padStart(2,"0")} • {c.aniversario_verificado?"✅ verificado":"⏳ pendente"}</p>}</button>)}</div></main>}
