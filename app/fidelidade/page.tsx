"use client";
import Link from"next/link";import{useEffect,useState}from"react";import MascotMoment from"../components/MascotMoment";const META=12,MINIMO=20;function normalizar(v:string){let t=String(v||"").replace(/\D/g,"");if(t.startsWith("55")&&t.length>=12)t=t.slice(2);if(t.startsWith("0")&&t.length>=11)t=t.slice(1);return t}export default function Fidelidade(){const[telefone,setTelefone]=useState(""),[selos,setSelos]=useState(0),[codigo,setCodigo]=useState(""),[status,setStatus]=useState("");async function resgate(t:string){try{const r=await fetch(`/api/resgates?telefone=${encodeURIComponent(t)}&t=${Date.now()}`,{cache:"no-store"}),j=await r.json();if(r.ok&&j.resgate)setCodigo(j.resgate.codigo||"")}catch{}}async function consultar(t:string){const tel=normalizar(t);if(tel.length<10)return;try{const r=await fetch(`/api/fidelidade?telefone=${encodeURIComponent(tel)}&t=${Date.now()}`,{cache:"no-store"}),j=await r.json();if(r.ok&&j.cliente){setSelos(Number(j.cliente.pontos||0));if(Number(j.novos_selos||0)>0)setStatus("⭐ Novo selo recebido com sucesso!");await resgate(tel)}}catch{}}useEffect(()=>{let t="";try{const d=JSON.parse(localStorage.getItem("popular_dados_cliente")||"null");t=normalizar(d?.telefone||"")}catch{}if(!t)t=normalizar(localStorage.getItem("popular-fidelidade-telefone")||"");if(t){setTelefone(t);void consultar(t)}},[]);useEffect(()=>{if(!telefone)return;const x=setInterval(()=>void consultar(telefone),10000);return()=>clearInterval(x)},[telefone]);const faltam=Math.max(META-selos,0),progresso=Math.min((selos/META)*100,100);return <main className="min-h-screen bg-zinc-950 text-white p-4 pb-28 max-w-2xl mx-auto"><header className="pt-3"><p className="text-yellow-400 font-bold text-sm">POPULAR HAMBURGUERIA E SORVETERIA</p><h1 className="text-3xl font-black mt-1">⭐ Clube Popular</h1><p className="text-zinc-400 mt-2">Pedidos pelo aplicativo com <b className="text-white">R$ 20,00 ou mais em itens elegíveis</b> ganham 1 selo.</p></header><div className="mt-4"><MascotMoment titulo="Seu Clube Popular ⭐" texto={selos>0?`Você já tem ${selos} selo${selos===1?"":"s"}. Continue juntando para chegar ao seu prêmio!`:"Faça pedidos elegíveis, junte 12 selos e ganhe seu X-Salada grátis!"} compacto/></div>{codigo&&<section className="mt-5 bg-gradient-to-br from-green-400 to-emerald-600 text-black rounded-3xl p-6 shadow-2xl text-center"><MascotMoment titulo="Parabéns! Seu prêmio está liberado!" texto="Você completou seu cartão do Clube Popular." compacto/><p className="text-xs font-black opacity-60 mt-2">🎁 PRÊMIO LIBERADO • 1 X-SALADA GRÁTIS</p><h2 className="text-2xl font-black mt-2">Seu código de resgate</h2><div className="bg-black text-yellow-400 rounded-2xl py-5 px-3 mt-4 font-mono text-3xl font-black tracking-wider select-all">{codigo}</div><p className="text-sm font-bold mt-4">🔐 Código individual e de uso único</p><p className="text-xs mt-1 opacity-75">Apresente este código presencialmente ou use o resgate em um pedido pelo aplicativo. Após a validação no Admin, ele não poderá ser usado novamente.</p></section>}<section className="mt-5 bg-yellow-400 text-black rounded-3xl p-5"><div className="flex justify-between"><div><p className="text-xs font-black opacity-60">SEU CARTÃO</p><h2 className="text-3xl font-black">{Math.min(selos,META)}/{META}</h2></div><div className="text-right"><p className="text-xs font-black opacity-60">FALTAM</p><b className="text-xl">{faltam}</b></div></div><div className="grid grid-cols-6 gap-2 mt-4 justify-items-center">{Array.from({length:META}).map((_,i)=><div key={i} className={`w-9 h-9 rounded-full flex items-center justify-center text-sm font-bold border ${i<selos?"bg-black text-yellow-400 border-black":"bg-white/25 border-black/10"}`}>{i<selos?"★":i+1}</div>)}</div><div className="h-2 bg-black/20 rounded-full mt-4 overflow-hidden"><div className="h-full bg-black rounded-full" style={{width:`${progresso}%`}}/></div></section>{status&&<div className="mt-3 bg-green-500/10 border border-green-500/30 rounded-2xl p-2 text-green-300"><MascotMoment titulo="Mais um selo para você!" texto="Seu novo selo já entrou no cartão. Continue assim!" compacto/></div>}<section className="mt-4 bg-zinc-900 border border-zinc-800 rounded-3xl p-5"><p className="text-yellow-400 text-xs font-black">COMO FUNCIONA</p><h2 className="text-xl font-black mt-1">R$ 20 em itens elegíveis = 1 selo</h2><p className="text-zinc-400 text-sm mt-2">Contam: <b className="text-white">lanches, batatas, refrigerantes, sucos e vitaminas.</b></p><p className="text-zinc-500 text-sm mt-2">Sorvetes, milk-shakes e açaís não contam.</p><div className="mt-4 bg-zinc-800 rounded-xl p-3 text-sm">⭐ 12 selos = <b>1 X-Salada grátis</b></div></section><details className="mt-5 bg-zinc-900 border border-zinc-800 rounded-3xl p-5"><summary className="font-black">📱 Consultar outro WhatsApp</summary><input value={telefone} onChange={e=>setTelefone(e.target.value)} inputMode="tel" placeholder="(38) 99999-9999" className="w-full mt-3 bg-zinc-800 p-3 rounded-xl"/><button onClick={()=>{const t=normalizar(telefone);if(t.length>=10){localStorage.setItem("popular-fidelidade-telefone",t);setCodigo("");void consultar(t)}}} className="w-full mt-3 bg-yellow-400 text-black py-3 rounded-xl font-black">Consultar selos</button></details><details className="mt-4 bg-zinc-900 border border-zinc-800 rounded-3xl p-5"><summary className="font-black">ℹ️ Regras do Clube Popular</summary><div className="text-sm text-zinc-400 mt-3 space-y-2"><p>• Promoção válida <b className="text-white">somente para pedidos realizados pelo aplicativo.</b></p><p>• Mínimo de <b className="text-white">R$ {MINIMO},00 em itens elegíveis</b>.</p><p>• Contam lanches, batatas, refrigerantes, sucos e vitaminas.</p><p>• Sorvetes, milk-shakes e açaís não contam.</p><p>• Cada pedido elegível gera no máximo 1 selo.</p><p>• {META} selos liberam 1 X-Salada grátis.</p><p>• Ao completar 12 selos, o sistema gera um <b className="text-white">código exclusivo de uso único</b>.</p><p>• O prêmio pode ser validado presencialmente ou por um pedido no aplicativo.</p><p>• Depois de utilizado, o código é bloqueado definitivamente.</p></div></details><Link href="/cardapio" className="block text-center mt-5 bg-yellow-400 text-black py-4 rounded-2xl font-black">🍔 Fazer pedido</Link></main>}
